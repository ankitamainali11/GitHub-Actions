name: hello-world

on: push 

jobs:
  myjob:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code 
        uses: actions/checkout@v4

      - name: my-step
        run: echo "Hello EVERYONE"
